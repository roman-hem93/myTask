package ru.artrostudio.mytask.controller

import androidx.appcompat.app.AppCompatActivity

class Controllers(activity: AppCompatActivity) {

    init {
        val controllerTasks = ControllerTasks(activity)
    }
}